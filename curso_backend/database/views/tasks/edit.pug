html 
 head
  title Tareas
body 
  form(action=`/tasks/${task.id}?_method=PUT` method="POST")
    textarea(name="description" placeholder="Pendiente")= task.description
    input(type="submit" value="Guardar")